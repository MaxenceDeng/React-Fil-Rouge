import React from "react";

const Footer = (props) => (
    <footer className="bg-dark text-white text-center p-4">
        @H2Prog - Tout droit reservé
    </footer>
);

export default Footer;
